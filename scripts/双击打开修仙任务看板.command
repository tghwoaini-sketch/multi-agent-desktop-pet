#!/bin/zsh

# Compatibility launcher for the old name. The pet is now the primary UI;
# keep one startup path so this legacy shortcut cannot reopen the retired dojo.
PROJECT_DIR="/Users/Admin/Documents/项目/交互看板"
exec "$PROJECT_DIR/scripts/启动小步修仙任务看板.command"
